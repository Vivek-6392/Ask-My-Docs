# ── Core ─────────────────────────────────────────────────────────────────────
streamlit>=1.35.0,<2.0.0
python-dotenv>=1.0.0

# ── LLM — Groq ───────────────────────────────────────────────────────────────
groq>=0.9.0
langchain-groq>=0.1.6

# ── LangChain core ───────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-community>=0.2.0
langchain-chroma>=0.1.2
langchain-text-splitters>=0.2.0

# ── Embeddings — HuggingFace local (no API key) ───────────────────────────────
langchain-huggingface>=0.0.3
sentence-transformers>=3.0.0   # used for both embeddings + reranker
torch>=2.3.0                   # CPU-only is fine; GPU if available

# ── Vector Store ──────────────────────────────────────────────────────────────
chromadb>=0.5.0
protobuf==3.20.3

# ── Sparse Retrieval ──────────────────────────────────────────────────────────
rank-bm25>=0.2.2

# ── Document Loaders ─────────────────────────────────────────────────────────
pypdf>=4.2.0
python-docx>=1.1.0
unstructured>=0.14.0
python-magic>=0.4.27

# ── Evaluation ────────────────────────────────────────────────────────────────
ragas>=0.1.14
datasets>=2.19.0

# ── Dev / Test ────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
ruff>=0.4.0
mypy>=1.10.0
